apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
labels:
  - pairs:
      environment: prod
patches:
  - path: service-account-patch.yaml
    target:
      kind: ServiceAccount
      name: secret-reader

# configMapGenerator:
#   - name: env-config
#     literals:
#       - ENVIRONMENT=prod

# replicas:
#   - name: webui
#     count: 3
